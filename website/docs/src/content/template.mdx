---
title: Start with a template
description: Start with a template
---

# Start with a template

GenSX provides several production-ready templates to help you build complex LLM applications quickly. Choose a template above to get started.

## Select a template

To get started, select one of the templates below:

<Tabs
  defaultTab="chat-ux"
  tabs={[
    {
      id: `chat-ux`,
      name: `Chat UX`,
      description: ``
    },
    {
      id: `deep-research`,
      name: `Deep Research`,
      description: ``
    },
    {
      id: `draft-pad`,
      name: `Draft Pad`,
      description: ``
    },
    {
      id: `client-side-tools`,
      name: `Client Side Tools`,
      description: `Example app for calling tools on the client side.`
    }
  ]}
>

<TabSection tab="chat-ux">
The [Chat UX template](https://github.com/gensx-inc/chat-ux-template) is a full-featured chat application with streaming, tools, and thread history.

<a href="https://chat-demo.gensx.com" target="_blank" rel="noopener noreferrer">
  <div className="my-4 w-full lg:w-5/6 mx-auto">
      <p className="text-sm text-gray-600 dark:text-gray-400 text-center mt-2 italic">
      Click to try the live demo
    </p>
    <div className="relative rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700">
      <Image
        src="/templates/chat-ux.png"
        alt="Chat UX template interface showing a chat application with streaming messages and tool integration"
        width={500}
        height={350}
        className="w-full h-auto"
      />
    </div>

  </div>
</a>

</TabSection>

<TabSection tab="deep-research">
The [Deep Research template](https://github.com/gensx-inc/deep-research-template) is a research assistant that can help you with your research.

<a href="https://deep-research-demo.gensx.com" target="_blank" rel="noopener noreferrer">
  <div className="my-4 w-full lg:w-5/6 mx-auto">
      <p className="text-sm text-gray-600 dark:text-gray-400 text-center mt-2 italic">
      Click to try the live demo
    </p>
    <div className="relative rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700">
      <Image
        src="/templates/deep-research.png"
        alt="Deep Research template interface showing research tools and analysis features"
        width={500}
        height={350}
        className="w-full h-auto"
      />
    </div>

  </div>
</a>

</TabSection>

<TabSection tab="draft-pad">
The [Draft Pad template](https://github.com/gensx-inc/draft-pad-template) is a collaborative writing and editing tool.

<a href="https://draft-pad-demo.gensx.com" target="_blank" rel="noopener noreferrer">
  <div className="my-4 w-full lg:w-5/6 mx-auto">
      <p className="text-sm text-gray-600 dark:text-gray-400 text-center mt-2 italic">
      Click to try the live demo
    </p>
    <div className="relative rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700">
      <Image
        src="/templates/draft-pad.png"
        alt="Draft Pad template interface showing collaborative writing and editing features"
        width={500}
        height={350}
        className="w-full h-auto"
      />
    </div>

  </div>
</a>

</TabSection>

<TabSection tab="client-side-tools">
The [Client Side Tools template](https://github.com/gensx-inc/client-side-tools-template) is a collection of AI-powered utilities that can be used in your application.

<a href="https://map-demo.gensx.com" target="_blank" rel="noopener noreferrer">
  <div className="my-4 w-full lg:w-5/6 mx-auto">
      <p className="text-sm text-gray-600 dark:text-gray-400 text-center mt-2 italic">
      Click to try the live demo
    </p>
    <div className="relative rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700">
      <Image
        src="/templates/map-agent.png"
        alt="Client Side Tools template interface showing AI-powered map and utility features"
        width={500}
        height={350}
        className="w-full h-auto"
      />
    </div>

  </div>
</a>

</TabSection>

## Log in to GenSX

If you haven't already, download the GenSX CLI and log in to GenSX using the following commands:

```bash
# Install the CLI
npm i -g gensx

# Log in to GenSX Cloud
gensx login
```

You’ll be redirected to the GenSX website and will need to create an account if you don’t have one already.

## Download the template

Next, download the template by running the following command:

<TabSection tab="chat-ux">
```bash
gensx template clone chat-ux
```

This will clone the template into a new directory called `chat-ux`.

Alternatively, you can [create your own GitHub repository from the template](https://github.com/gensx-inc/chat-ux-template/generate) and then clone it yourself.

</TabSection>

<TabSection tab="deep-research">
```bash
gensx template clone deep-research
```

This will clone the template into a new directory called `deep-research`.

Alternatively, you can [create your own GitHub repository from the template](https://github.com/gensx-inc/deep-research-template/generate) and then clone it yourself.

</TabSection>

<TabSection tab="draft-pad">
```bash
gensx template clone draft-pad
```

This will clone the template into a new directory called `draft-pad`.

Alternatively, you can [create your own GitHub repository from the template](https://github.com/gensx-inc/draft-pad-template/generate) and then clone it yourself.

</TabSection>

<TabSection tab="client-side-tools">
```bash
gensx template clone client-side-tools
```

This will clone the template into a new directory called `client-side-tools`.

Alternatively, you can [create your own GitHub repository from the template](https://github.com/gensx-inc/client-side-tools-template/generate) and then clone it yourself.

</TabSection>

## Run the template

Now that you've cloned the template, navigate to the template directory:

<TabSection tab="chat-ux">
```bash
cd chat-ux
```

Configure the required API keys for your template:

```bash
export TAVILY_API_KEY=your-api-key
export ANTHROPIC_API_KEY=your-api-key
```
</TabSection>

<TabSection tab="deep-research">
```bash
cd deep-research
```

Configure the required API keys for your template:

```bash
export TAVILY_API_KEY=your-api-key
export ANTHROPIC_API_KEY=your-api-key
export OPENAI_API_KEY=your-api-key
```
</TabSection>

<TabSection tab="draft-pad">
```bash
cd draft-pad
```

Configure the required API keys for your template:

```bash
export TAVILY_API_KEY=your-api-key
export ANTHROPIC_API_KEY=your-api-key
```
</TabSection>

<TabSection tab="client-side-tools">
```bash
cd client-side-tools
```

Configure the required API keys for your template:

```bash
export TAVILY_API_KEY=your-api-key
export ANTHROPIC_API_KEY=your-api-key
```
</TabSection>

</Tabs>

Start the development server:

```bash
npm run dev
```
