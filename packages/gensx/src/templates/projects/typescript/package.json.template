{
  "name": "my-gensx-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "npm build && node dist/index.js",
    "start": "npx gensx start ./src/workflows.ts",
    "build": "tsc -p tsconfig.build.json",
    "deploy": "npx gensx deploy -ev OPENAI_API_KEY ./src/workflows.ts",
    "prepare": "ts-patch install -s"
  }
}
